import { Stack, Typography } from "@mui/material"

export const Books = () => {
  return (
    <Stack className="w-full h-full bg-primary-light">
      <Stack className="w-full h-full bg-primary">
      <Typography>Get started with over 200 books</Typography>
      </Stack>
      

    </Stack>
  )
}
